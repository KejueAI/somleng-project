# Somleng Platform - Production Configuration
# Copy this file to .env and update the values before running setup.sh

# =============================================================================
# REQUIRED SETTINGS
# =============================================================================

# Your domain name (e.g., chorus-ai.co)
# Caddy will automatically obtain Let's Encrypt TLS certificates.
#
# DNS RECORDS REQUIRED (set these BEFORE starting):
#   A record:  <DOMAIN>    → <server-public-ip>
#   A record:  *.<DOMAIN>  → <server-public-ip>
#
# The wildcard record enables subdomain-based routing:
#   apisip.<DOMAIN>                    → Twilio-compatible REST API
#   appsip.<DOMAIN>                    → Main dashboard
#   <carrier>.appsip.<DOMAIN>          → Carrier dashboards
#   verifysip.<DOMAIN>                 → Verify API
DOMAIN=somleng.example.com

# Public IP address of this server (required for SIP/VoIP)
# Used by FreeSWITCH for SIP signaling and RTP media.
FS_EXTERNAL_SIP_IP=
FS_EXTERNAL_RTP_IP=

# =============================================================================
# SECRETS (auto-generated by setup.sh if left empty)
# =============================================================================

POSTGRES_PASSWORD=
SECRET_KEY_BASE=
ANYCABLE_SECRET=
RATING_ENGINE_PASSWORD=

# =============================================================================
# OPTIONAL SETTINGS
# =============================================================================

# SMS Gateway device token (leave empty to disable SMS gateway)
SMS_GATEWAY_DEVICE_TOKEN=

# PostgreSQL settings
POSTGRES_USER=postgres
DB_POOL=30

# Rating engine settings
RATING_ENGINE_USERNAME=cgrates

# AWS region (used by the application for S3, Polly, etc.)
AWS_REGION=eu-central-1

# Rails environment
RAILS_ENV=production
